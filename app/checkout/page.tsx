import Link from 'next/link'
export default function Checkout({searchParams}:{searchParams:{plan?:string}}){const full=searchParams.plan==='full';const name=full?'FULL EVENT PASS':'DAILY PASS';const price=full?'Rp99.000':'Rp29.000';return <main className="container" style={{padding:'60px 0',maxWidth:720}}><Link className="muted" href="/">← Back</Link><div className="card" style={{padding:32,marginTop:24}}><span className="muted">CHECKOUT</span><h1 style={{fontSize:40,margin:'10px 0'}}>{name}</h1><div style={{fontSize:30,fontWeight:800}}>{price}</div><p className="muted">Pembayaran aman melalui payment gateway. Setelah pembayaran terverifikasi, akses live akan otomatis aktif.</p><form action="/api/checkout" method="post"><input type="hidden" name="plan" value={full?'full':'daily'}/><label className="muted">Email</label><input required name="email" type="email" placeholder="you@example.com" style={{display:'block',width:'100%',margin:'8px 0 18px',padding:14,borderRadius:10,border:'1px solid #303642',background:'#080b10',color:'white'}}/><button className="btn btn-primary" style={{width:'100%',cursor:'pointer'}}>Continue to Payment</button></form></div></main>}
