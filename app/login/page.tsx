'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function Login(){const [email,setEmail]=useState('');const [code,setCode]=useState('');const [sent,setSent]=useState(false);const [error,setError]=useState('');const router=useRouter();async function request(){setError('');const r=await fetch('/api/auth/request-otp',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});const d=await r.json();if(!r.ok)return setError(d.error||'Unable to send code');setSent(true)}async function verify(){setError('');const r=await fetch('/api/auth/verify-otp',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,code})});const d=await r.json();if(!r.ok)return setError(d.error||'Invalid code');router.push('/live')}return <main className="container" style={{minHeight:'100vh',display:'grid',placeItems:'center',maxWidth:620}}><div className="card" style={{padding:32,width:'100%'}}><span className="muted">RUMGIP LIVE</span><h1 style={{fontSize:40,margin:'10px 0'}}>Sign in to watch.</h1><p className="muted">We will send a one-time verification code to your email.</p>{!sent?<><input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com" className="field"/><button onClick={request} className="btn btn-primary" style={{width:'100%',marginTop:14}}>Send code</button></>:<><input value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,'').slice(0,6))} inputMode="numeric" placeholder="6-digit code" className="field"/><button onClick={verify} className="btn btn-primary" style={{width:'100%',marginTop:14}}>Verify & continue</button><button onClick={()=>setSent(false)} className="btn btn-ghost" style={{width:'100%',marginTop:10}}>Change email</button></>}{error&&<p style={{color:'#ff8d8d'}}>{error}</p>}</div></main>}
